"use client";

import { motion } from "framer-motion";

export const MotionDiv = motion.div;
//now this is the only client component and we can keep it separate and use it in animecard, as it has server components 